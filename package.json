{
  "name": "node-socket-keepalive-test",
  "version": "1.0.0",
  "description": "Simple socket server and client for testing socket keepalive behavior.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "test",
    "socket",
    "tcp",
    "keepalive"
  ],
  "author": "Travis Sinnott",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.1.0"
  }
}
